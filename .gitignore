# Large binary assets
.env
/VRM/
インストーラー/
node_modules/

# Next.js build output (kit を 2 プロセスで dev 起動するため A/B 別に出す)
aituber-kit/.next/
aituber-kit/.next-*/
aituber-kit/logs/

# Local logs
duel_log.jsonl
duel_events.jsonl

# config.json のバックアップ（admin 保存時に作成される）
config.json.bak

# VRM ファイル（GitHub の 100MB 制限を超えるため、リポジトリには含めない。
#  ユーザー各自で aituber-kit/public/vrm/ に配置する想定）
*.vrm
